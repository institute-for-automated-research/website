.vercel
.env
.env.*
!.env.example

# python
__pycache__/
*.pyc

# node / astro (wiki)
node_modules/
dist/
.astro/

# ISSN submission confirmations contain personal phone/home address — public repo
docs/issn-attachments/issn-*-submission-*.pdf
docs/issn-iar-a-zip-correction.txt

# OpenAlex refs cache (regenerable via ground-relatesto.mjs)
scripts/openalex/.refs-cache/

# Crossref resolve cache (regenerable via check-relatesto-dois.mjs)
scripts/openalex/.crossref-cache/

# Claude Code runtime lock (transient: sessionId/pid/timestamp, not for VCS)
.claude/scheduled_tasks.lock
.zeropaper-empirical-mirror/
